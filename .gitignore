# Environment variables
.env
.env.local
.env.*.local
# Docker is permanently excluded from this project (PM2 + direct deploy only)

# Dependencies
node_modules/
mcp-venv/
data/mcp/

# Build outputs
dist/
build/
*.tsbuildinfo

# Logs
*.log
logs/

# OS files
**/.DS_Store
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Testing
coverage/
.jest-cache/
test-results/

# Runtime cache artifacts
backend/api/cache/

# Vite
.vite/

# AI Skills (large repos, installed via npx)
.gemini/
.antigravitycli/
.agent/
.spawner/
.claude/
.superpowers/
.cursorrules
.ruff_cache/

# Runtime data (databases, user data, usage stats)
/data/
!/data/.gitkeep
backend/api/data/

# Legacy backup files
bak/
backup/

# User uploaded files (sensitive user data)
backend/uploads/
uploads/

# OCR trained data (large binary, download separately)
*.traineddata

# Python cache
__pycache__/
*.pyc

# IDE workspace files
*.code-workspace

# Runtime config (auto-generated)
.ollama-cluster.json
backend/api/.ollama-cluster.json

# Screenshots (may contain personal info)
*.png
*.jpg
*.jpeg
!frontend/web/public/**/*.png
!frontend/web/public/**/*.jpg
!frontend/web/public/**/*.jpeg
!apps/web/public/**/*.png
!apps/web/public/**/*.jpg
!apps/web/public/**/*.jpeg
!apps/web/app/icon.png
!screenshot-main.png
!skill-library-current.png
# README screenshots/demo assets (personal info masked before commit)
!assets/**/*.png

# Project Documentation
openmake_prd.md

# Compiled Scripts (scripts/*.ts 의 tsc 산출물 — 소스가 아닌 것만 무시)
scripts/*.js
scripts/*.map
scripts/*.d.ts
# 예외: 아래는 컴파일 산출물이 아니라 소스다 (빌드 파이프라인이 직접 실행 — 커밋 필수)
!scripts/build-info.js

# Tool-generated plans/specs
docs/superpowers/

# Playwright MCP logs
.playwright-mcp/

# Bun
bun.lock
bunfig.toml

# Tests & Dev Configs
**/__tests__/
tests/
*.test.ts
*.spec.ts
playwright.config.ts
.eslintignore
.eslintrc.json

# Dev Documentation
SCHEMA_SOURCE_OF_TRUTH.md
user-guide.md
.omc/

# 운영 무관 개발 전용 문서/지침 (저장소 미추적, 로컬 보존)
/docs/
/CLAUDE.md
/AGENTS.md
/apps/web/CLAUDE.md
/apps/web/AGENTS.md
/apps/web/README.md

# AI 생성 이미지 (generate_image 도구 출력)
frontend/web/public/generated/
# 모노레포 재편(frontend/web → apps/legacy-web) 후 현행 경로.
# 생성 이미지 + 예약 리포트 게시 산출물(reports/<slug>/*.html)이 매일 쌓이는 런타임 디렉토리.
apps/legacy-web/public/generated/

# Archives / temp bundles (e.g. prompt.zip)
*.zip
*.tar
*.tar.gz
*.tgz

# 로컬 curl 응답 덤프 (수동 디버깅 산출물 — 커밋 대상 아님)
body.txt
tbody.txt
up_chat.json
up_models.json

# Cloudflare Tunnel 설정 — git 커밋 금지 지침 (라이브: ~/.cloudflared/config.yml)
scripts/cloudflared/
data/desktop-updates/

# 도구 세션 상태(run-continuation) — 저장소와 무관
.omo/
