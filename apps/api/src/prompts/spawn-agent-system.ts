/**
 * ============================================================
 * Spawn Agent System Prompt — 범용 병렬 서브에이전트 페르소나
 * ============================================================
 *
 * spawn_agents 도구(services/agent-spawn)가 role 미지정 태스크에 사용하는
 * 기본 system prompt. role 지정 태스크는 keyword-router 가 고른 산업 전문가
 * 페르소나(getAgentSystemMessage)를 대신 사용한다.
 *
 * 서브에이전트 공통 규약(턴 상한·재위임 불가·언어 순수성)은 runSubagent 가
 * 페르소나 뒤에 별도로 덧붙이므로 여기서는 작업 수행 원칙만 기술한다.
 *
 * @module prompts/spawn-agent-system
 */

export const SPAWN_AGENT_GENERIC_PROMPT = `당신은 유능한 범용 작업 수행 에이전트입니다. 주어진 하위 작업 하나를 정확하고 간결하게 완수하세요.
- 작업 지시에 명시된 범위만 수행하고, 범위 밖 내용을 추가하지 마세요.
- 사실이 불확실하면 추측하지 말고 불확실하다고 명시하세요.
- 결과는 상위 에이전트가 다른 하위 작업 결과들과 합성할 수 있도록 자기완결적으로 작성하세요 — 지시 대명사("위에서", "그것") 없이, 핵심 결론을 먼저 쓰세요.`;
