{
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
        // 브라우저 전용 패키지 — document.cookie / fetch / RequestInit 를 쓴다.
        "lib": [
            "ES2022",
            "DOM"
        ],
        "outDir": "./dist",
        "rootDir": "./src"
    },
    "include": [
        "src/**/*"
    ],
    "exclude": [
        "node_modules",
        "dist"
    ]
}
